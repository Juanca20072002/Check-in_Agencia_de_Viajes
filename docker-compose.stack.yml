services:
  db:
    image: postgres:16-alpine
    container_name: viajes_db
    environment:
      POSTGRES_USER: reservas
      POSTGRES_PASSWORD: reservas
      POSTGRES_DB: reservasdb
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U reservas -d reservasdb -h localhost"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    image: ${DOCKERHUB_USERNAME:-juancaing}/check-in-agencia:${APP_IMAGE_TAG:-latest}
    container_name: viajes_web
    restart: unless-stopped
    environment:
      # Cadena directa para SQLAlchemy (principal)
      DATABASE_URL: postgresql://reservas:reservas@db:5432/reservasdb
      # Fallbacks usados por app.py si faltara DATABASE_URL
      POSTGRES_USER: reservas
      POSTGRES_PW: reservas
      POSTGRES_DB: reservasdb
      POSTGRES_HOST: db
      POSTGRES_PORT: "5432"
      FLASK_SECRET_KEY: ${FLASK_SECRET_KEY}
      FLASK_ENV: production
      PORT: "5000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "80:5000"
    volumes:
      - uploads:/app/static/img

volumes:
  pgdata:
  uploads:
